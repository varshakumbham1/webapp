name: Node.js Integration Tests

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:5.7
        env:
          MYSQL_ROOT_PASSWORD: Geethareddy@1989
          MYSQL_DATABASE: sequelize_db
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 14

    - name: Wait for MySQL to be ready
      run: |
        until mysqladmin ping -h mysql -u root -proot_password; do
          echo 'Waiting for MySQL to become available...'
          sleep 5
        done

    - name: Install dependencies
      run: npm install
      
    - name: Start the Node.js application
      run: npm start

    - name: Run integration tests
      run: npm test
